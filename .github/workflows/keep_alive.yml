# .github/workflows/keep_alive.yml
name: Keep Bot Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # A cada 5 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot to keep alive
        run: |
          curl -s -o /dev/null "https://chkbot-0lqr.onrender.com/health"
